# Platform API

NestJS REST API for Math Learning Platform.

All setup, environment variables, and standard commands live in the root README: [`../../README.md`](../../README.md).

Run only this app (from repo root):

```bash
bun run dev --filter=platform-api
```
